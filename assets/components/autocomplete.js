export default class Autocomplete{

    /**
     *
     * @param {HTMLElement|null} element
     */
    constructor(element) {
        if (element === null){
            return
        }
        console.log('under construction');
    }
}